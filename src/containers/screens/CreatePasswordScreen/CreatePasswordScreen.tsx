import React from "react";
import {MainWrapperView, TitleScreenText} from "./CreatePasswordScreenStyles";

const CreatePasswordScreen: React.VFC = () => {
  return (
    <MainWrapperView>
      <TitleScreenText>CreatePasswordScreen</TitleScreenText>
    </MainWrapperView>
  );
};

export default CreatePasswordScreen;
